# Nimbus environment template (demo / future production)
# Copy to apps/web/.env.local as needed.

# Public site URL (Cloudflare Pages)
NEXT_PUBLIC_SITE_URL=https://nimbus.example.pages.dev

# Payments (production) — demo currently uses mock Paystack
# NEXT_PUBLIC_PAYSTACK_PUBLIC_KEY=
# PAYSTACK_SECRET_KEY=

# Database (optional — catalog is currently in-repo; wire when API lands)
# DATABASE_URL=
